#coding:utf-8
"""
Semana 4 – Ejercicio 1
Escriba un programa que pida dos números enteros y que calcule
su división, escribiendo si la división es exacta o no.
"""
print 'ingrese el dividendo y el dividor'
print ''
num1 = float(raw_input('Escriba el dividendo: '))
num2 = float(raw_input('Escriba el divisor: '))
modulo = num1 % num2
resul = num1 / num2
if modulo == 0:
    print 'division exacta', resul

if modulo != 0:
    print 'division inexacta', resul
